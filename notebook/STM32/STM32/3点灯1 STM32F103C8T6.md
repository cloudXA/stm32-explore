# 点灯

* * *

```c
void HAL_GPIO_Init(GPIO_TypeDef *GPIOx, GPIO_InitTypeDef *GPIO_Init);
void HAL_GPIO_WritePin(GPIO_TypeDef *GPIOx, uint16_t GPIO_Pin, GPIO_PinState PinState)
void HAL_GPIO_TogglePin(GPIO_TypeDef *GPIOx, uint16_t GPIO_Pin)
__HAL_RCC_GPIOA_CLK_ENABLE();
__HAL_RCC_GPIOB_CLK_ENABLE();


/** Configure pins as
        * Analog
        * Input
        * Output
        * EVENT_OUT
        * EXTI
*/
void MX_GPIO_Init(void)
{

  GPIO_InitTypeDef GPIO_InitStruct = {0};

  /* GPIO Ports Clock Enable */
  __HAL_RCC_GPIOA_CLK_ENABLE();
  __HAL_RCC_GPIOB_CLK_ENABLE();

  /*Configure GPIO pin Output Level */
  HAL_GPIO_WritePin(GPIOB, GPIO_PIN_8|GPIO_PIN_9, GPIO_PIN_RESET);

  /*Configure GPIO pins : PB8 PB9 */
  GPIO_InitStruct.Pin = GPIO_PIN_8|GPIO_PIN_9;
  GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;
  GPIO_InitStruct.Pull = GPIO_NOPULL;
  GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_LOW;
  HAL_GPIO_Init(GPIOB, &GPIO_InitStruct);

}
```



你贴出的这段代码，是 **STM32CubeMX 工具自动生成的 GPIO 初始化函数**。它非常经典且严谨，完美融合了你之前问到的 **时钟使能**、**Init 结构体** 和 **WritePin** 三个核心知识点。

我帮你把这段代码的执行逻辑拆解成 **“三步走”**，并特别指出一个**非常专业的细节亮点**（专业工程师和爱好者的分水岭）。

---

### 1. 代码执行流程（分步解析）

- **第 1 步：局部变量清零（防御性编程）**
  ```c
  GPIO_InitTypeDef GPIO_InitStruct = {0};
  ```
  将配置结构体初始化为全 0。这防止了结构体中未赋值的成员（如 `Alternate` 复用功能位）包含内存中的随机垃圾值，避免意外配置成未知模式。

- **第 2 步：开启外设时钟（供电）**
  ```c
  __HAL_RCC_GPIOA_CLK_ENABLE();
  __HAL_RCC_GPIOB_CLK_ENABLE();
  ```
  - 正如之前所讲，这是**操作 GPIO 的绝对前提**。
  - **注意**：下面只配置了 PB8/PB9，因此点灯本身只需要 GPIOB 时钟。GPIOA 时钟通常是因为工程中还有其他 PA 引脚被使用，或者是 CubeMX 根据整体配置生成；不能仅凭这段代码断定它是为 SWD 打开的。SWD 引脚由调试功能配置，普通点灯代码不应随意关闭或重映射它们。

- **第 3 步：预置初始电平（极其优雅的处理）**
  ```c
  HAL_GPIO_WritePin(GPIOB, GPIO_PIN_8|GPIO_PIN_9, GPIO_PIN_RESET);
  ```
  **这里隐含了一个重要顺序**：`WritePin` 写在 `HAL_GPIO_Init` **之前**。

- **第 4 步：正式应用配置**
  ```c
  GPIO_InitStruct.Pin = GPIO_PIN_8|GPIO_PIN_9;
  GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP; // 推挽输出
  GPIO_InitStruct.Pull = GPIO_NOPULL;         // 无上下拉
  GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_LOW;// 低速
  HAL_GPIO_Init(GPIOB, &GPIO_InitStruct);
  ```
  结构体赋值后，调用 `HAL_GPIO_Init` 将配置真正写入物理寄存器。这里是 STM32F103（F1系列），主要配置的是 `CRL/CRH`；输出电平涉及 `ODR/BSRR/BRR`。`MODER/OTYPER/PUPDR/OSPEEDR` 是较新的STM32系列常见命名，不适用于F103。

---

### 2. ⭐ 代码里的“隐藏大神操作”（重点！）

你可能会疑惑：**为什么要把 `HAL_GPIO_WritePin` 写在 `HAL_GPIO_Init` 前面？** 

绝大多数新手会先把引脚初始化（Init）完，再去 WritePin 置电平。但 CubeMX 生成的代码故意把 WritePin 放在前面，这是一个**极其高级的防抖/防毛刺策略**：

- **GPIO 的输出数据寄存器（ODR）** 和 **配置寄存器（F103上的CRL/CRH）** 是独立的。
- 如果在 `Init` 之后才 WritePin，那么在 `Init` 生效的那一瞬间（纳秒级），引脚会先**以默认的硬件状态（可能是高阻或随机电平）输出一瞬间**，然后再被 WritePin 拉低。
- **现在的做法**：先把 ODR 寄存器里的对应位**预置为 0**，然后再去 Init 开启输出模式。这样，当输出模式生效的那一刹那，引脚内部直接锁存的是你预设好的低电平——**实现了“零抖动”上电，外接的 LED 不会在开机时闪烁一下，继电器不会误吸合。**

---

### 3. 这段代码的参数解读（为什么这样配？）

| 参数 | 值 | 含义与适用场景 |
| :--- | :--- | :--- |
| **Mode** | `GPIO_MODE_OUTPUT_PP` | **推挽输出**：能稳定输出 0V 和 3.3V，驱动能力强，适用于驱动 LED、普通数字信号。 |
| **Pull** | `GPIO_NOPULL` | **无上下拉**：因为是输出模式，电平由 MCU 主动控制，内部上下拉电阻只会增加功耗，所以关闭。 |
| **Speed** | `GPIO_SPEED_FREQ_LOW` | **低速配置**：它主要控制输出驱动的边沿能力/最大切换速度，不代表引脚会自动输出2MHz。LED等低速负载优先选低速，可减小不必要的快速边沿和EMI。 |

---

### 4. 配套“反初始化”思考（拔电）

如果哪天你要让这两个引脚进入最低功耗模式（比如停机待机），CubeMX 通常会生成对应的 `HAL_GPIO_DeInit` 调用：
```c
HAL_GPIO_DeInit(GPIOB, GPIO_PIN_8|GPIO_PIN_9);
```
  这会把相关GPIO配置恢复到该系列芯片定义的复位状态。对STM32F1普通GPIO而言，复位状态通常是浮空输入；不能把所有系列都概括成“自动变为模拟输入”。若要最低功耗，应根据具体型号手册显式配置未使用引脚。

---

### 📌 总结一句话
> **这段代码是工程级 GPIO 初始化的范本**：先开时钟 -> 预置电平防止抖动 -> 填结构体参数 -> 加载到硬件。它不仅是“能用”，而且是“好用且稳定”的典范。**记住这个“先写电平、再初始化”的顺序，是判断代码功底深浅的重要标志之一。**

## 实验步骤与验证

1. 在CubeMX中把PB8配置为推挽输出，确认开发板LED是高电平亮还是低电平亮。
2. 生成代码后，在初始化完成处分别写 `GPIO_PIN_SET` 和 `GPIO_PIN_RESET`，观察LED状态。
3. 在主循环中每500ms调用一次 `HAL_GPIO_TogglePin()`，确认能够周期闪烁。
4. 若不亮，按“GPIOB时钟 → PB8模式 → LED有效电平 → 原理图 → 共地与供电”顺序排查。

## 必须区分的两个概念

- `GPIO_PIN_SET/RESET` 描述的是引脚电压高低。
- “LED亮/灭”由LED接法决定。若LED接在3.3V与PB8之间，PB8输出低电平时吸收电流，LED反而点亮，这叫低电平有效。

