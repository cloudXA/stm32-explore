## 25 看门狗实验
### 开启独立看门狗,溢出时间为1秒,使用按键1进行喂狗
### 硬件接线
![alt text](image-5.png) 
KEY1 -- PA0
UART1 -- PA9 PA10
### 溢出时间计算
#### 公式: ![alt text](image-6.png)
#### PSC: ![alt text](image-7.png)
#### f1wdg: ![alt text](image-8.png)
#### 1000毫秒: ![alt text](image-9.png)
#### 溢出时间设置: PSC=64, RLR=625
### STM32CubeMX配置
![alt text](image-10.png)
![alt text](image-11.png)
![alt text](image-12.png)
![alt text](image-13.png)
![alt text](image-14.png)
![alt text](image-15.png)
![alt text](image-16.png)
![alt text](image-17.png)
![alt text](image-18.png)
![alt text](image-19.png)
![alt text](image-20.png)

### 核心代码

#### main.c 主函数

```c
int main(void)
{
  /* 初始化硬件 */
  HAL_Init();
  SystemClock_Config();
  MX_GPIO_Init();
  MX_IWDG_Init();          // 初始化独立看门狗
  MX_USART1_UART_Init();

  /* USER CODE BEGIN 2 */
  HAL_UART_Transmit(&huart1, "program start\n", strlen("program start\n"), 100);
  /* USER CODE END 2 */

  /* 主循环 */
  while (1)
  {
    // 检测按键 KEY1 (PA0) 是否被按下
    if(HAL_GPIO_ReadPin(GPIOA, GPIO_PIN_0) == GPIO_PIN_RESET) {
        HAL_IWDG_Refresh(&hiwdg);   // 喂狗：刷新看门狗计数器
        HAL_Delay(50);              // 按键消抖
    }
  }
}
```

#### 看门狗初始化 (iwdg.c)

```c
IWDG_HandleTypeDef hiwdg;

void MX_IWDG_Init(void)
{
  hiwdg.Instance = IWDG;
  hiwdg.Init.Prescaler = IWDG_PRESCALER_64;  // 预分频系数 64
  hiwdg.Init.Reload = 625;                   // 重装载值 625
  if (HAL_IWDG_Init(&hiwdg) != HAL_OK)
  {
    Error_Handler();
  }
}
```

### 代码逻辑说明

#### 1. 看门狗的作用

独立看门狗 (IWDG) 是一个倒计时计数器。程序必须在计数器减到 0 之前"喂狗"（刷新计数器），否则看门狗会认为程序跑飞，自动复位整个单片机。

```text
程序正常运行 → 定时喂狗 → 计数器被重置 → 不会复位
程序卡死/跑飞 → 没人喂狗 → 计数器减到 0 → 单片机复位
```

#### 2. 溢出时间计算

看门狗溢出时间由两个参数决定：

```text
溢出时间 = (预分频系数 / LSI频率) × 重装载值
         = (64 / 40000) × 625
         = 1.6ms × 625
         = 1000ms = 1秒
```

- **LSI 频率**：40kHz（内部低速时钟）
- **Prescaler = 64**：把 40kHz 分成 40000/64 = 625Hz，即每个计数 1.6ms
- **Reload = 625**：625 个计数 × 1.6ms = 1 秒

所以：**1 秒内不喂狗，单片机就会复位**。

#### 3. 喂狗逻辑

```c
if(HAL_GPIO_ReadPin(GPIOA, GPIO_PIN_0) == GPIO_PIN_RESET) {
    HAL_IWDG_Refresh(&hiwdg);   // 按下按键才喂狗
    HAL_Delay(50);              // 消抖，防止一次按键触发多次
}
```

- `GPIO_PIN_RESET` 表示按键按下（低电平有效，PA0 默认上拉）
- 按下 KEY1 → 喂一次狗 → 计数器重置回 625
- `HAL_Delay(50)` 用于按键消抖

**实验现象**：
- 一直按着 KEY1 → 持续喂狗 → 程序不会复位
- 松开 KEY1，超过 1 秒不喂狗 → 看门狗复位 → 串口重新打印 "program start"

#### 4. 为什么用独立看门狗 (IWDG)

| 特性 | 独立看门狗 (IWDG) |
|------|------------------|
| 时钟源 | LSI（内部 40kHz，独立于主时钟） |
| 独立性 | 即使主时钟/系统时钟故障也能工作 |
| 一旦开启 | 无法软件关闭，只能复位后关闭 |
| 适用场景 | 检测程序跑飞、死循环等严重故障 |

独立看门狗由独立的 LSI 时钟驱动，不受主系统时钟影响。即使程序因为时钟配置错误或死循环而完全失控，看门狗仍然能正常倒计时并在超时后复位系统，是单片机"最后一道保险"。
